// RepoStats - Content Script
// Runs on GitHub pages to detect repo information

console.log('RepoStats content script loaded');

// Simple content script - just needs to exist
// The real work happens in popup.js when user clicks the icon
